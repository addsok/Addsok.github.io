export default function AdminImportPage() {
  return (
    <div className="card">
      <h1 className="text-2xl font-bold">Admin Import Deprecated</h1>
      <p className="mt-2 text-sm text-gray-300">
        Shared BO7 weapon/camo content now ships from local TypeScript data files and no longer uses Supabase import/seed flows.
      </p>
    </div>
  );
}
